/**
 * Placeholder exports for @bolivia-fantasy/scoring.
 * The pure scoring engine (FPL-style points rules for the Bolivian
 * División Profesional) will live here.
 */
export const SCORING_PACKAGE = "@bolivia-fantasy/scoring";

/** Placeholder: always returns 0 points until the real rules land. */
export function calculatePoints(): number {
  return 0;
}
